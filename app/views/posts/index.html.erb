<h1>投稿一覧表</h1>

<table>
  <tr>
    <th>内容</th>
  </tr>

  <% @post.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= link_to "詳細を確認する", post_path(post.id) %></td>
      <td><%= link_to "詳細を確認する", edit_post_path(post.id) %></td>
    </tr>
  <% end %>
<table>
<%= link_to "新しく投稿する", new_post_path %>